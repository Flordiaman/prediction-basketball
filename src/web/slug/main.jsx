import React from "react";
import ReactDOM from "react-dom/client";
import SlugPage from "./SlugPage.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SlugPage />
  </React.StrictMode>
);
