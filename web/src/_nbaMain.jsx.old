import React from "react";
import { createRoot } from "react-dom/client";

import "./index.css"; // ✅ match main.jsx styling foundation
import NbaDbPanel from "./components/NbaDbPanel";

function NbaStandalone() {
  return (
    <div className="min-h-screen bg-neutral-900 text-neutral-100 p-6">
      <div className="max-w-6xl mx-auto">
        <div className="mb-4">
          <div className="text-2xl font-extrabold">Blurift — NBA</div>
          <div className="opacity-75 mt-1">
            Standalone view (nba.html → nbaMain.jsx)
          </div>
        </div>

        <div className="rounded-xl border border-white/10 bg-white/5 p-4">
          <NbaDbPanel />
        </div>
      </div>
    </div>
  );
}

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NbaStandalone />
  </React.StrictMode>
);
